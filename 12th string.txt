Write a C++ program to accept and display string entered by the user and performs String operations on it using following string functions.
a. strcat()
b. strrev() c. strcmp()
d. strlen( e. strchr()
#include<iostream>
#include<cstring>

using namespace std;

int main() {
    char str1[50], str2[50];
    cout << "Enter first string: ";
    cin >> str1;

    cout << "Enter second string: ";
    cin >> str2;

    // Appending str2 to str1 using strcat()
    strcat(str1, str2);
    cout << "After appending, the string is: " << str1 << endl;

    // Reversing str1 using strrev()
    strrev(str1);
    cout << "After reversing, the string is: " << str1 << endl;

    // Comparing str1 and str2 using strcmp()
    int comparison = strcmp(str1, str2);
    if (comparison == 0)
        cout << "Both strings are identical." << endl;
    else if (comparison > 0)
        cout << "str1 is greater than str2." << endl;
    else
        cout << "str1 is less than str2." << endl;

    // Finding the length of str1 using strlen()
    int length = strlen(str1);
    cout << "The length of the string is: " << length << endl;

    // Finding the first occurrence of 'a' in str1 using strchr()
    char* firstOccurrence = strchr(str1, 'a');
    if (firstOccurrence)
        cout << "The first occurrence of 'a' is at position: " << (firstOccurrence - str1) +
